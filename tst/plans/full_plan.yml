name: Plan with options and variables for testing
options:
  session: yes
variables:
  hostname: google.com
variable_files:
- vars.yml
requests:
- name: "Load {{ hostname }}"
  get:
    url: "https://{{ hostname }}"
- name: "Load wikipedia.org"
  get:
    url: "https://wikipedia.org"
